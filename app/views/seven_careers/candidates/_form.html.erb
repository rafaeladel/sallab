<%= form_for([@job, @candidate], :html => {:multipart => true}) do |f| %>
  <% if @candidate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@candidate.errors.count, "error") %> prohibited this candidate from being saved:</h2>

      <ul>
      <% @candidate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    Firstname <%= f.text_field :firstname %>
    Lastname <%= f.text_field :lastname%>
    Current Salary <%= f.number_field :current_salary %>
    Email <%= f.email_field :email %>
    Phone <%= f.phone_field :phone %>
    Resume <%= f.file_field :resume %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
