<%= form_for(@product_section) do |f| %>
  <% if @product_section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_section.errors.count, "error") %> prohibited this product_section from being saved:</h2>

      <ul>
      <% @product_section.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <%= f.label :title_en %><br>
      <%= f.text_field :title_en %>
    </div>
    <div class="field">
  <%= f.label :title_ar %><br>
  <%= f.text_field :title_ar %>
</div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
